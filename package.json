{
  "dependencies": {},
  "devDependencies": {
    "parcel": "^1.12.4",
    "pug": "^2.0.4",
    "sass": "^1.26.5",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "build": "parcel build --no-source-maps -d . -o index markup.pug",
    "clean": "bash -c 'rm -f *.*.{css,jpg,js} index.html'",
    "serve": "parcel serve -d .serve -p 8080 markup.pug"
  }
}
