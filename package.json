{
  "devDependencies": {
    "parcel": "^1.12.4",
    "pug": "^3.0.0",
    "sass": "^1.26.11",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "build": "parcel build --log-level 4 --no-source-maps --public-url /dist src/markup.pug",
    "clean": "rm -r dist",
    "serve": "node serve.js"
  }
}
